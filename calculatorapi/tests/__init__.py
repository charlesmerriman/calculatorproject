"""
Tests for calculatorapi, one module per feature area.

Every test class inherits CalculatorTestCase from base.py, which starts each
test with an empty cache. Builders that several modules share live in
factories.py; a helper only one module uses stays in that module.
"""
